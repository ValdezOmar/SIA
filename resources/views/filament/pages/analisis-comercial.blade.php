<x-filament-panels::page>
    <div class="rounded-xl bg-white p-4 shadow-sm ring-1 ring-gray-950/5 dark:bg-gray-900 dark:ring-white/10 sm:p-6">
        <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-lg font-semibold text-gray-950 dark:text-white">Análisis comercial</h2><p class="text-sm text-gray-500 dark:text-gray-400">Ventas contabilizadas del mes seleccionado.</p></div><select wire:model.live="periodo" class="rounded-lg border-gray-300 bg-white text-sm text-gray-900 dark:border-gray-700 dark:bg-gray-800 dark:text-white">@foreach($this->periodos() as $valor => $etiqueta)<option value="{{ $valor }}">{{ $etiqueta }}</option>@endforeach</select></div>
        <div class="mt-5 flex flex-wrap gap-2">@foreach(['vendidos'=>'Más vendidos','rentables'=>'Más rentables','clientes'=>'Clientes','resumen'=>'Resumen'] as $clave=>$texto)<button wire:click="seleccionar('{{ $clave }}')" class="rounded-lg px-3 py-2 text-sm font-medium {{ $pestana===$clave ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-200' }}">{{ $texto }}</button>@endforeach</div>
        <div class="mt-5 overflow-x-auto"><table class="w-full text-sm"><tbody class="divide-y divide-gray-200 dark:divide-white/10">@forelse($filas as $fila)<tr><td class="py-3">@if(isset($fila['codigo']))<div class="flex min-w-72 items-center gap-3"><div class="h-16 w-16 shrink-0 overflow-hidden rounded-lg bg-gray-100 dark:bg-gray-800">@if($fila['foto'])<img src="{{ $fila['foto'] }}" alt="" class="h-full w-full object-cover">@else<span class="flex h-full items-center justify-center text-2xl">📦</span>@endif</div><div class="min-w-0 leading-tight"><div class="truncate text-sm font-semibold text-gray-950 dark:text-white">{{ $fila['codigo'] }}</div><div class="truncate text-xs text-gray-500 dark:text-gray-400">Modelo: {{ $fila['modelo'] ?: 'Sin modelo' }}</div><div class="truncate text-xs text-gray-700 dark:text-gray-200">{{ $fila['principal'] }}</div><div class="text-xs font-semibold {{ ($fila['stock'] ?? 0) > 5 ? 'text-success-600' : (($fila['stock'] ?? 0) > 1 ? 'text-warning-600' : 'text-danger-600') }}">{{ ($fila['stock'] ?? 0) > 0 ? 'Stock: '.number_format($fila['stock'], 2, ',', '.') : 'Stock: Sin stock' }}</div><div class="truncate text-xs text-gray-500 dark:text-gray-400">Marca: {{ $fila['marca'] ?: 'Sin marca' }}</div></div></div>@else<div class="font-medium text-gray-900 dark:text-white">{{ $fila['principal'] }}</div>@endif</td><td class="py-3 text-right text-gray-700 dark:text-gray-200">{{ $fila['valor'] }}</td></tr>@empty<tr><td class="py-8 text-center text-gray-500">No hay ventas contabilizadas para este mes.</td></tr>@endforelse</tbody></table></div>
    </div>
</x-filament-panels::page>
